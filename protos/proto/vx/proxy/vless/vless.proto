syntax = "proto3";
package vx.proxy.vless;
option go_package = "buf.build/gen/go/vvvvv/vx/protocolbuffers/go/vx/proxy";
import "vx/user/user.proto";

message VlessClientConfig {
  // ID of the account, in the form of a UUID, e.g.,
  // "66ad4540-b58c-4ad2-9926-ea63445a9b57".
  string id = 5;
  // Flow settings. May be "xtls-rprx-vision".
  string flow = 6;
  // Encryption settings. Only applies to client side, and only accepts "none"
  // for now.
  string encryption = 7;
}

message VlessServerConfig {
  repeated vx.user.UserConfig users = 1;
}